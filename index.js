const amount = document.getElementById("Amount").value;
const currency1 = document.getElementById("currency1").value;
const currency2 = document.getElementById("currency2").value;

const convert = document.getElementById("convert").value;
const message = document.getElementById("message").value;

const botton = document.getElementById("btn").value;

currency1.addEventListener('change', conversion).value;
currency2.addEventListener('change', conversion).value;
amount.addEventListener('input', conversion).value;
convert.addEventListener('input', conversion).value;
message.addEventListener('input', conversion).value;

function conversion(){
    
}